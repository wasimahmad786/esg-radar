bundle:
  name: esg-radar

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    esg-radar-app:
      name: "esg-radar"
      description: "esg-radar created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
